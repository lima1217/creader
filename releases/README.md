# Local release staging

This folder holds **local** macOS `.dmg` files before upload to GitHub Releases. Binaries here are gitignored (`releases/**/*.dmg`).

See [docs/release.md](../docs/release.md) for the build and publish workflow.

Download published builds from [GitHub Releases](https://github.com/lima1217/creader/releases).
